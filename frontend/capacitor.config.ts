import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.interviewer.app',
  appName: 'AI Virtual HR Interviewer',
  webDir: 'dist/frontend/browser'
};

export default config;
